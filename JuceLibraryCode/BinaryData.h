/* =========================================================================================

   This is an auto-generated file: Any edits you make may be overwritten!

*/

#pragma once

namespace BinaryData
{
    extern const char*   frogs_wav;
    const int            frogs_wavSize = 1226184;

    extern const char*   scabbers_wav;
    const int            scabbers_wavSize = 2757156;

    extern const char*   scabbers2_wav;
    const int            scabbers2_wavSize = 231576;

    extern const char*   alter_ego_wav;
    const int            alter_ego_wavSize = 1110798;

    extern const char*   frenzy_wav;
    const int            frenzy_wavSize = 180174;

    extern const char*   frozen_sticks_wav;
    const int            frozen_sticks_wavSize = 2471256;

    extern const char*   time_freeze_wav;
    const int            time_freeze_wavSize = 1826046;

    extern const char*   witches_wav;
    const int            witches_wavSize = 1609620;

    extern const char*   clang_wav;
    const int            clang_wavSize = 118594;

    extern const char*   clap_wav;
    const int            clap_wavSize = 60478;

    extern const char*   clonck_wav;
    const int            clonck_wavSize = 71560;

    extern const char*   Crack1_wav;
    const int            Crack1_wavSize = 939364;

    extern const char*   crack2_wav;
    const int            crack2_wavSize = 167014;

    extern const char*   Zish_wav;
    const int            Zish_wavSize = 2950210;

    extern const char*   zissl_wav;
    const int            zissl_wavSize = 282754;

    extern const char*   Zosh_wav;
    const int            Zosh_wavSize = 2511646;

    extern const char*   Smudge_png;
    const int            Smudge_pngSize = 19314;

    // Number of elements in the namedResourceList and originalFileNames arrays.
    const int namedResourceListSize = 17;

    // Points to the start of a list of resource names.
    extern const char* namedResourceList[];

    // Points to the start of a list of resource filenames.
    extern const char* originalFilenames[];

    // If you provide the name of one of the binary resource variables above, this function will
    // return the corresponding data and its size (or a null pointer if the name isn't found).
    const char* getNamedResource (const char* resourceNameUTF8, int& dataSizeInBytes);

    // If you provide the name of one of the binary resource variables above, this function will
    // return the corresponding original, non-mangled filename (or a null pointer if the name isn't found).
    const char* getNamedResourceOriginalFilename (const char* resourceNameUTF8);
}
